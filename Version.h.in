#pragma once

// ─────────────────────────────────────────────────────────────────────────────
//  canvas/Version.h  — AUTO-GENERATED by CMake. DO NOT EDIT.
//  Source: cmake/Version.h.in
// ─────────────────────────────────────────────────────────────────────────────

#include <cstdint>
#include <string_view>

namespace canvas {

inline constexpr uint32_t VERSION_MAJOR = @Canvas_VERSION_MAJOR@;
inline constexpr uint32_t VERSION_MINOR = @Canvas_VERSION_MINOR@;
inline constexpr uint32_t VERSION_PATCH = @Canvas_VERSION_PATCH@;

/// Packed version: 0xMMmmPP (major, minor, patch each 1 byte)
inline constexpr uint32_t VERSION =
    (VERSION_MAJOR << 16) | (VERSION_MINOR << 8) | VERSION_PATCH;

inline constexpr std::string_view VERSION_STRING = "@Canvas_VERSION@";
inline constexpr std::string_view PROJECT_NAME   = "@PROJECT_NAME@";
inline constexpr std::string_view BUILD_TYPE     = "@CMAKE_BUILD_TYPE@";
inline constexpr std::string_view PLATFORM       = "@CANVAS_PLATFORM@";
inline constexpr std::string_view GPU_BACKEND    = "@CANVAS_GPU_BACKEND@";

/// Returns true when this build was compiled with AddressSanitizer.
#ifdef __SANITIZE_ADDRESS__
inline constexpr bool ASAN_ENABLED = true;
#else
inline constexpr bool ASAN_ENABLED = false;
#endif

} // namespace canvas
